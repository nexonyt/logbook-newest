import styled from 'styled-components';
import { NavLink } from 'react-router-dom';

export const StyledNavLink = styled(NavLink)`
  color: #333;
  text-decoration: none;
  padding: 0.5rem 1rem;
  display: flex; /* żeby ikonka i tekst były w linii */
  align-items: center;

  &.active {
    color: #007bff; /* kolor aktywnej zakładki */
    font-weight: 600;
    border-bottom: 2px solid #007bff; /* np. podkreślenie aktywnej */
  }

  &:hover {
    color: #0056b3;
  }
`;
